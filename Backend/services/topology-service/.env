# Local environment variables for topology-service
# Keep the database connection string only in .env, not in appsettings.json.
ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=INMS.Topology;Username=postgres;Password=X5w7@post#123