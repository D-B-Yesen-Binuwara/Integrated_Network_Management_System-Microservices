# The React app calls the gateway, not individual microservices.
VITE_API_BASE_URL=http://localhost:5253

# Keep false until the authentication flow and CSRF protection are enabled.
VITE_API_USE_COOKIES=false
